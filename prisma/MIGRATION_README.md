-- Prisma Data Migration baseline
-- Created: 2026-08-19
-- This migration reflects the schema state after Phase 1-4 hardening.
-- Run with: prisma migrate deploy (production) or prisma migrate dev (development)

-- Note: This file is intentionally minimal because Prisma's migrate diff
-- generates the actual SQL. To create a real migration, run:
--
--   DATABASE_URL="postgresql://user:pass@host:5432/db?schema=public" \
--     bunx prisma migrate dev --name init --create-only
--
-- Then review the generated SQL in prisma/migrations/*/migration.sql.

--noop
